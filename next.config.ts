import type {NextConfig} from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['images.unsplash.com', 'assets.aceternity.com', 'aceternity.com','cdn.sanity.io'],
    },
    /* config options here */
};

export default nextConfig;
